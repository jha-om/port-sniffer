[package]
name = "port_sniffer"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
bpaf = { version = "0.9", features = ["autocomplete", "derive", "bright-color"] }
